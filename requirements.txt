# Crawler
feedparser==6.0.11
httpx==0.27.0
trafilatura==1.12.0
beautifulsoup4==4.12.3
lxml

# Database
supabase>=2.10.0          # 2.10+ supports new Supabase publishable/service key format

# AI — summarization & classification
groq==0.11.0

# Translation fallback (offline) — install separately if needed:
# pip install argostranslate

# Extractive summarization fallback — install separately if needed:
# pip install sumy

# Newsletter
resend==2.3.0
jinja2==3.1.4

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0
langdetect==1.0.9
tqdm==4.66.4
